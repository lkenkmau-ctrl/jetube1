# ⚡ Быстрый старт - Деплой на Netlify + Supabase

## 1. Настройка Supabase (5 минут)

1. Зарегистрируйтесь на https://supabase.com
2. Создайте новый проект
3. В **SQL Editor** выполните содержимое файла `supabase-schema.sql`
4. Скопируйте из **Settings → API**:
   - Project URL
   - anon/public key

## 2. Настройка фронтенда (2 минуты)

Откройте `frontend/.env` и вставьте ваши данные:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-ключ
```

## 3. Деплой на Netlify (5 минут)

1. Закоммитьте файлы в Git:
   ```bash
   git add .
   git commit -m "Ready for Netlify"
   git push
   ```

2. На https://app.netlify.com:
   - **Add new site → Import from GitHub**
   - Выберите репозиторий
   - Build settings:
     - Base directory: `frontend`
     - Build command: `npm run build`
     - Publish directory: `dist`
   - Добавьте Environment variables:
     - `VITE_SUPABASE_URL`
     - `VITE_SUPABASE_ANON_KEY`

3. Нажмите **Deploy**

## ✅ Готово!

Ваш сайт доступен по адресу `https://ваш-сайт.netlify.app`

---

## 📁 Созданные файлы

| Файл | Назначение |
|------|------------|
| `netlify.toml` | Конфигурация сборки Netlify |
| `frontend/.env` | Переменные окружения Supabase |
| `frontend/src/lib/supabase.js` | Supabase клиент |
| `frontend/src/App.jsx` | Обновлённое приложение |
| `supabase-schema.sql` | Схема базы данных |
| `DEPLOY.md` | Подробная инструкция |
| `migrate-to-supabase.js` | Скрипт миграции данных |

## 🔄 Что изменилось

- ❌ Бэкенд на Node.js больше не нужен
- ❌ Файловая БД `database.json` заменена на PostgreSQL
- ✅ Все данные в облаке (Supabase)
- ✅ Статический фронтенд на Netlify
- ✅ Аутентификация через Supabase Auth
- ✅ Файлы хранятся в Supabase Storage

## ⚠️ Важно

- Старые пользователи должны сбросить пароль (через Supabase)
- Видеофайлы нужно загрузить заново через интерфейс
- Все API вызовы теперь через Supabase клиент
